#include <stdio.h>

int main() {
    
    int base = 22;
    int high = 35;

        float area = base * high / 2;
        printf("the area of triangle with base %d and high %d is: %.2f \n",base, high,area);

        return 0;
}